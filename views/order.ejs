<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
  <title>Order Receipt - Supermarket App</title>
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mt-4">
    <h2>Order #<%= order.id %></h2>
    <p>Status: <strong><%= order.status %></strong></p>
    <p>Placed: <%= new Date(order.created_at).toLocaleString() %></p>

    <h4>Items</h4>
    <table class="table">
      <thead><tr><th>Product</th><th>Qty</th><th>Price</th><th>Subtotal</th></tr></thead>
      <tbody>
        <% let total = 0; %>
        <% items.forEach(i => { %>
          <tr>
            <td><%= i.productName %></td>
            <td><%= i.quantity %></td>
            <td>$<%= i.unit_price.toFixed(2) %></td>
            <td>$<%= (i.unit_price * i.quantity).toFixed(2) %></td>
          </tr>
          <% total += i.unit_price * i.quantity; %>
        <% }); %>
      </tbody>
    </table>

    <h4>Total: $<%= total.toFixed(2) %></h4>

    <h4>Shipping</h4>
    <% if (shipping) { %>
      <p><strong><%= shipping.recipient_name %></strong><br>
      <%= shipping.address_line1 %><br>
      <% if (shipping.address_line2) { %><%= shipping.address_line2 %><br><% } %>
      <%= shipping.city %> <%= shipping.postal_code %></p>
    <% } else { %>
      <p>No shipping details provided.</p>
    <% } %>

    <a href="/shopping" class="btn btn-secondary">Continue Shopping</a>
  </div>
  <script src="/js/ui.js"></script>
</body>
</html>
